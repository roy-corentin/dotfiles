#!/usr/bin/env bash

## launch wofi with alt config

STYLE="$HOME/.config/wofi/style.css"

if [[ ! $(pidof wofi) ]]; then
	wofi --show drun --width=35% --prompt 'Search...' --columns 2 -I --style "${STYLE}"
else
	pkill wofi
fi
